/* Generated by CIL v. 1.3.7 */
/* print_CIL_Input is true */

struct epic_rx_desc {
   unsigned int next ;
};
struct epic_private {
   struct epic_rx_desc *rx_ring ;
   unsigned int rx_skbuff[5] ;
};
extern  __attribute__((__nothrow__, __noreturn__)) void abort(void)  __attribute__((__leaf__)) ;
static void epic_init_ring(struct epic_private *ep ) 
{ int i ;

  {
  i = 0;
  while (i < 5) {
    (ep->rx_ring + i)->next = (unsigned int )(10 + (i + 1) * 2);
    ep->rx_skbuff[i] = 0U;
    i ++;
  }
  (ep->rx_ring + (i - 1))->next = 10U;
  return;
}
}
static int check_rx_ring[5]  = {      12,      14,      16,      18, 
        10};
int main(void) 
{ struct epic_private ep ;
  struct epic_rx_desc rx_ring[5] ;
  int i ;

  {
  i = 0;
  while (i < 5) {
    rx_ring[i].next = 0U;
    ep.rx_skbuff[i] = 5U;
    i ++;
  }
  ep.rx_ring = rx_ring;
  epic_init_ring(& ep);
  i = 0;
  while (i < 5) {
    if (rx_ring[i].next != (unsigned int )check_rx_ring[i]) {
      abort();
    }
    if (ep.rx_skbuff[i] != 0U) {
      abort();
    }
    i ++;
  }
  return (0);
}
}
