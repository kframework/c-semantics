/* Generated by CIL v. 1.3.7 */
/* print_CIL_Input is true */

extern void abort(void) ;
extern void exit(int  ) ;
int aim_callhandler(int sess , int conn , unsigned short family , unsigned short type ) ;
int aim_callhandler(int sess , int conn , unsigned short family , unsigned short type ) ;
static int i  =    0;
int aim_callhandler(int sess , int conn , unsigned short family , unsigned short type ) 
{ int tmp ;

  {
  if (! conn) {
    return (0);
  }
  if ((int )type == 65535) {
    return (0);
  }
  if (i >= 1) {
    abort();
  }
  i ++;
  tmp = aim_callhandler(sess, conn, family, (unsigned short)65535);
  return (tmp);
}
}
int main(void) 
{ 

  {
  aim_callhandler(0, 1, (unsigned short)0, (unsigned short)0);
  exit(0);
}
}
