/* Generated by CIL v. 1.3.7 */
/* print_CIL_Input is true */

extern  __attribute__((__nothrow__, __noreturn__)) void abort(void)  __attribute__((__leaf__)) ;
void scale(long *alpha , long *x , int n ) 
{ int i ;
  int ix ;
  long tmpr ;
  long tmpi ;

  {
  if (*alpha != 1L) {
    i = 0;
    ix = 0;
    while (i < n) {
      tmpr = *alpha * *(x + ix);
      tmpi = *alpha * *(x + (ix + 1));
      *(x + ix) = tmpr;
      *(x + (ix + 1)) = tmpi;
      i ++;
      ix += 2;
    }
  }
  return;
}
}
int main(void) 
{ int i ;
  long x[10] ;
  long alpha ;

  {
  alpha = 2L;
  i = 0;
  while (i < 10) {
    x[i] = (long )i;
    i ++;
  }
  scale(& alpha, x, 5);
  if (x[9] != 18L) {
    abort();
  }
  return (0);
}
}
