/* Generated by CIL v. 1.3.7 */
/* print_CIL_Input is true */

void ( __attribute__((__noinline__)) f)(long *limit , long *base , long minLen , long maxLen ) ;
void ( __attribute__((__noinline__)) f)(long *limit , long *base , long minLen , long maxLen ) 
{ long i ;
  long vec ;

  {
  vec = 0L;
  i = minLen;
  while (i <= maxLen) {
    vec += *(base + (i + 1L)) - *(base + i);
    *(limit + i) = vec - 1L;
    i ++;
  }
  return;
}
}
extern void abort(void) ;
extern void exit(int  ) ;
long b[4]  = {      1L,      5L,      11L,      23L};
int main(void) 
{ long l[3] ;

  {
  f(l, b, 0L, 2L);
  if (l[0] != 3L) {
    abort();
  } else {
    if (l[1] != 9L) {
      abort();
    } else {
      if (l[2] != 21L) {
        abort();
      }
    }
  }
  exit(0);
}
}
