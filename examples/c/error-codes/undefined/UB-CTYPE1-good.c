#include <ctype.h>
#include <stdio.h>

int main() {
      isalnum('X');
      isalnum(EOF);

      isalpha('X');
      isalpha(EOF);

      isblank('X');
      isblank(EOF);

      iscntrl('X');
      iscntrl(EOF);

      isdigit('X');
      isdigit(EOF);

      isgraph('X');
      isgraph(EOF);

      islower('X');
      islower(EOF);

      isprint('X');
      isprint(EOF);

      ispunct('X');
      ispunct(EOF);

      isspace('X');
      isspace(EOF);

      isupper('X');
      isupper(EOF);

      isxdigit('X');
      isxdigit(EOF);

      tolower('X');
      tolower(EOF);

      toupper('X');
      toupper(EOF);
}
